import { Typography } from "@mui/material";

export default function Error(){
    return (
        <div className="section" id="error">
        <Typography component='h3' variant="h2">Esta sección no exitse.</Typography>
        <Typography component='h3' variant="h2"></Typography>
        </div>
    )
}